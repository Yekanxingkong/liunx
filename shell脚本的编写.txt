#/bin/bash
调用执行系统

黑屏终端打开sh脚本命令./脚本名称

变量命令
your_name="qinjx"
echo $your_name


my_array=(A B "C" D)

echo "第一个元素为: ${my_array[0]}"
echo "第二个元素为: ${my_array[1]}"
echo "第三个元素为: ${my_array[2]}"
echo "第四个元素为: ${my_array[3]}"

echo "-- \$@ 演示 ---"
for i in "$@"; do
    echo $i
done